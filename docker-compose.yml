version: "3.8"

services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - instance:/app/instance
      - ./data:/app/data
      - ./reports:/app/reports
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

volumes:
  instance: